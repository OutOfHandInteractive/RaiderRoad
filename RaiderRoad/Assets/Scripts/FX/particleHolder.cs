using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class particleHolder : MonoBehaviour
{
    //A script designed to hold a particle. If the player needs to access a particle specific to a certain object, this script will hold it
    //This script may be replaced or expanded later; it's main use it when respawning players in-game

    public ParticleSystem myParticle;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
